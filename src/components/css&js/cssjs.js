import React from "react";
import './cssjs.css'

const cssjs = (props) =>{
    const x= `front_setter`
    return(
    <h>
        <div className={`${x} color_set`}>css styling combining with js and react</div>
    </h>
    )
}
export default cssjs;